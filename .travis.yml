language: r
pandoc_version: 1.17.2
dist: precise
cache: packages

addons:
  apt:
    packages:
    - texlive-lang-japanese，
    - texlive-luatex

before_script:
  - tlmgr install luatexja
script:
  - lualatex test.tex
