language: r
pandoc_version: 1.17.2
dist: trusty

branches:
  only:
    - master

addons:
  apt:
    packages:
    - fonts-ipaexfont
    - texlive-luatex
    - texlive-lang-japanese

script:
  - lualatex --output-directory=doc test.tex
